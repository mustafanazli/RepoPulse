namespace RepoPulse.UnitTests;

// Infrastructure smoke test only (RP-001) — verifies the test project builds and
// runs under dotnet test / CI. Replace with real scoring-engine tests in Faz 2.
public class PlaceholderTests
{
    [Fact]
    public void TestProject_IsWiredUpAndRunnable()
    {
        Assert.True(true);
    }
}
